DATABASE_URL="postgresql://postgres.<project-ref>:<password>@aws-0-eu-west-1.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1"
DIRECT_URL="postgresql://postgres:<password>@db.<project-ref>.supabase.co:5432/postgres"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
AUTH_EMAIL_PROVIDER="smtp"
NEXT_PUBLIC_SUPABASE_URL=""
NEXT_PUBLIC_SUPABASE_ANON_KEY=""

# ── Email (Resend — preferred, works on Vercel) ────────────────────────────
# Get your API key from https://resend.com/api-keys
RESEND_API_KEY=""
EMAIL_FROM="Akunta <noreply@akunta.se>"
SUPPORT_EMAIL=""

# ── Email (SMTP — local dev / self-hosted only, blocked on Vercel) ─────────
# STRATO: smtp.strato.de + port 465 (SSL/TLS)
SMTP_HOST=""
SMTP_PORT="465"
SMTP_SECURE="true"
SMTP_USER=""
SMTP_PASS=""

# ── Security ───────────────────────────────────────────────────────────────
# Generate with: openssl rand -hex 32
SESSION_SALT=""
# Generate with: openssl rand -hex 32
EMAIL_WEBHOOK_SECRET=""

# ── AI / OCR ──────────────────────────────────────────────────────────────
OPENAI_API_KEY=""
OPENAI_RECEIPT_MODEL="gpt-4.1-mini"

# ── Local dev single-user login (not used in production) ──────────────────
AKUNTA_LOGIN_USERNAME=""
AKUNTA_LOGIN_PASSWORD=""
AKUNTA_SESSION_TOKEN="akunta-local-session"
